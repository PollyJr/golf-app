import { ChevronRight, Flag, MapPin, Route } from "lucide-react";
import { courses } from "@/lib/demo-data";
export default function CoursesPage(){return <div className="page"><div className="page-heading"><div><div className="eyebrow"><span/> onze banen</div><h1>Elke ronde een nieuw verhaal.</h1><p>Bekijk baaninformatie, holes, tees en afstanden.</p></div></div><div className="course-detail-grid">{courses.map((course,index)=><article className="card course-detail" key={course.id}><div className={`course-cover cover-${index}`}><span className="badge"><Flag size={10}/>{course.holes} holes</span><div className="course-cover-number">{String(index+1).padStart(2,"0")}</div></div><div className="course-detail-body"><h2>{course.name}</h2><p>{course.description}</p><div className="course-facts"><span><b>{course.totalPar}</b>Par</span><span><b>{course.holes}</b>Holes</span><span><b>{course.tee}</b>Tee</span></div><div className="hole-strip">{course.layout.slice(0,9).map(h=><span key={h.number}><b>{h.number}</b><small>{h.distance}m · P{h.par}</small></span>)}</div><button><Route size={16}/> Bekijk baaninformatie <ChevronRight size={16}/></button></div></article>)}</div></div>}
