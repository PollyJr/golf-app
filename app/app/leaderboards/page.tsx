"use client";
import { useState } from "react";
import { ArrowDown, ArrowUp, Minus } from "lucide-react";
import { leaderboard } from "@/lib/demo-data";
import { formatToPar } from "@/lib/score";
import type { Period } from "@/lib/types";

export default function LeaderboardsPage(){const [period,setPeriod]=useState<Period>("week");return <div className="page"><div className="page-heading"><div><div className="eyebrow"><span/> clubklassement</div><h1>Wie staat er bovenaan?</h1><p>Beste goedgekeurde ronde ten opzichte van par.</p></div><div className="tabs">{(["day","week","month","year"] as Period[]).map(p=><button key={p} onClick={()=>setPeriod(p)} className={period===p?"active":""}>{({day:"Dag",week:"Week",month:"Maand",year:"Jaar"})[p]}</button>)}</div></div><div className="my-position"><strong>#2</strong><span className="avatar">SV</span><div><b>Jouw positie</b><p>Beste ronde · Shortgolf Twente</p></div><div className="position-score"><strong>25</strong><span>−2 par</span></div></div><div className="filters"><div className="tabs mobile-period">{(["day","week","month","year"] as Period[]).map(p=><button key={p} onClick={()=>setPeriod(p)} className={period===p?"active":""}>{({day:"Dag",week:"Week",month:"Maand",year:"Jaar"})[p]}</button>)}</div><div><select className="select" aria-label="Baan"><option>Alle 9-hole banen</option><option>Shortgolf Twente</option><option>De Heidebaan</option></select> <select className="select" aria-label="Holes"><option>9 holes</option><option>18 holes</option></select></div></div><div className="card leaderboard-card"><div className="leader-head"><span>Pos.</span><span>Speler</span><span>Baan</span><span>Rondes</span><span>Score</span></div>{leaderboard.map(entry=><div key={entry.id} className={`leader-row ${entry.id==="me"?"me":""}`}><span className={`rank ${entry.rank<=3?"podium":""}`}>{entry.rank}</span><div className="leader-person"><span className="avatar">{entry.initials}</span><span><b>{entry.name}</b><small>{entry.movement>0?<><ArrowUp size={9}/> {entry.movement} plaats</>:entry.movement<0?<><ArrowDown size={9}/> {Math.abs(entry.movement)} plaats</>:<><Minus size={9}/> gelijk</>}</small></span></div><span className="leader-course">{entry.course}</span><span className="leader-rounds">{entry.rounds} rondes</span><span className="leader-score"><strong>{entry.score}</strong><span>{formatToPar(entry.toPar)}</span></span></div>)}</div><p className="ranking-note">Alleen door de club goedgekeurde rondes tellen mee. Gelijke scores delen dezelfde positie.</p></div>}
